<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>HOB IT Mangunan</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;800&display=swap" rel="stylesheet">
  <style>
    /* 1. Aturan Dasar & Font Modern */
    :root {
      --primary-color: #6A5ACD; /* Biru Ungu (Trendy) */
      --secondary-color: #FF69B4; /* Hot Pink (Aksen Enerjik) */
      --text-dark: #333333;
      --bg-light: #F4F7F8;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      scroll-behavior: smooth;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background-color: var(--bg-light);
      color: var(--text-dark);
      line-height: 1.6;
    }

    /* 2. Header Stylish */
    header {
      background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
      color: white;
      padding: 15px 30px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      position: sticky;
      top: 0;
      z-index: 1000;
    }

    .logo {
      font-size: 1.8em;
      font-weight: 800;
      letter-spacing: 1px;
    }

    .menu a {
      color: white;
      text-decoration: none;
      font-weight: 600;
      text-transform: uppercase;
      margin-left: 25px;
      transition: color 0.3s ease;
    }

    .menu a:hover {
      color: var(--bg-light);
      text-shadow: 0 0 5px rgba(255, 255, 255, 0.5);
    }

    /* 3. Main Content Container */
    main {
      padding: 40px 20px;
      max-width: 1000px;
      margin: 30px auto;
    }

    /* Gaya untuk Setiap Section */
    .content-section {
      background-color: white;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
      padding: 40px;
      margin-bottom: 40px;
    }

    .content-section h2 {
      color: var(--primary-color);
      font-size: 2em;
      font-weight: 800;
      margin-bottom: 20px;
      border-bottom: 3px solid var(--secondary-color);
      padding-bottom: 10px;
    }

    .highlight {
      color: var(--secondary-color);
      font-weight: 600;
    }

    .card-list {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      margin-top: 20px;
    }

    /* Gaya Card Participant */
    .participant-card {
      background-color: var(--bg-light);
      padding: 20px;
      border-radius: 10px;
      flex: 1 1 calc(33% - 20px); 
      text-align: center;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
      transition: transform 0.3s ease;
      cursor: pointer; /* Menandakan bisa diklik */
    }

    .participant-card:hover {
      transform: translateY(-5px) scale(1.02);
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
    }

    .participant-card h3 {
      color: var(--primary-color);
      margin-top: 10px;
      margin-bottom: 5px;
      font-size: 1.2em;
    }
    
    .participant-card img {
        width: 80px;
        height: 80px;
        border-radius: 50%; /* Membuat gambar menjadi lingkaran */
        object-fit: cover;
        border: 3px solid var(--secondary-color);
    }

    /* 4. Footer Minimalis */
    footer {
      background-color: var(--text-dark);
      color: var(--bg-light);
      text-align: center;
      padding: 20px 0;
      font-size: 0.9em;
    }
  </style>
</head>

<body>
  <header>
    <div class="logo">HOB IT</div>
    <div class="menu">
      <a href="#about">ABOUT</a>
      <a href="#participant">PARTICIPANT</a>
      <a href="#contact">CONTACT</a>
    </div>
  </header>

  <main>
    <section id="about" class="content-section">
      <h2>🌐 About HOB IT Mangunan</h2>
      <p>
        Selamat datang di **Himpunan Orang Bertalenta IT (HOB IT)** Mangunan! Kami adalah komunitas bagi para pelajar, mahasiswa, dan profesional muda yang memiliki <span class="highlight">passion tinggi terhadap dunia teknologi dan pengembangan digital</span>. Misi kami adalah menjadi wadah kolaborasi, berbagi ilmu, dan menghasilkan karya inovatif.
      </p>
      <div class="card-list">
        <div class="participant-card">
          <h3>Visi</h3>
          <p>Menciptakan ekosistem IT yang adaptif dan inovatif.</p>
        </div>
        <div class="participant-card">
          <h3>Misi</h3>
          <p>Mendorong kolaborasi dan edukasi teknologi bagi anak muda.</p>
        </div>
      </div>
    </section>

    <section id="participant" class="content-section">
      <h2>👥 Our Participants</h2>
      <p>
        Berikut adalah profil anggota aktif kami. Klik pada kartu untuk melihat profil sosial mereka!
      </p>

      <div class="card-list" id="participant-list">
        </div>
      
      <p style="text-align: center; margin-top: 30px;">
        <span class="highlight">Tertarik bergabung?</span> Kami selalu terbuka untuk talenta baru!
      </p>
    </section>

    <section id="contact" class="content-section">
      <h2>📞 Contact Us</h2>
      <p>Hubungi kami melalui email: <span class="highlight">hello@hobit.id</span> atau ikuti media sosial kami.</p>
    </section>
    
  </main>

  <footer>
    &copy; 2025 HOB IT Mangunan. Built with <span class="highlight" style="color: var(--secondary-color);">Passion</span>.
  </footer>

<script>
    // 1. Fungsi untuk membuat elemen Card
    function createParticipantCard(participant) {
      // Buat elemen <a> yang akan bertindak sebagai card (agar bisa diklik)
      const cardLink = document.createElement('a');
      cardLink.href = participant.url_sosial; // URL sosial saat card diklik
      cardLink.target = "_blank"; // Buka di tab baru
      cardLink.className = 'participant-card';

      // Buat HTML untuk Card Peserta
      cardLink.innerHTML = `
        <img src="${participant.url_gambar}" alt="Foto ${participant.nama}">
        <h3>${participant.nama}</h3>
        <p>${participant.judul}</p>
        <p style="font-size: 0.9em; opacity: 0.7;">"${participant.quote}"</p>
      `;
      return cardLink;
    }

    // 2. Fungsi untuk Mengambil (Fetch) Data dari File JSON Eksternal
    function fetchAndRenderParticipants() {
      const listContainer = document.getElementById('participant-list');
      
      // Menggunakan fetch() untuk mengambil data dari file participants.json
      fetch('participants.json')
        .then(response => {
          // Cek apakah response OK (status 200)
          if (!response.ok) {
            throw new Error('Gagal mengambil data peserta. Status: ' + response.status);
          }
          return response.json(); // Mengubah response menjadi objek JavaScript (JSON)
        })
        .then(participantsData => {
          // Loop data yang sudah diubah dari JSON
          participantsData.forEach(participant => {
            const card = createParticipantCard(participant);
            listContainer.appendChild(card);
          });
        })
        .catch(error => {
          // Menampilkan pesan error jika proses fetch gagal
          listContainer.innerHTML = `<p style="color: red;">Error memuat data: ${error.message}</p>`;
          console.error('Error fetching participant data:', error);
        });
    }

    // Panggil fungsi saat dokumen selesai dimuat
    document.addEventListener('DOMContentLoaded', fetchAndRenderParticipants);
  </script>
</body>
</html>
